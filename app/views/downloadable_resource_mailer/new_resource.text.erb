As-Salaam-Alaikum <%= @user.name %>,

We’re excited to let you know that a new resource(<%= @resource.name %>) has been added to QUL! You can access it using the link below:

Download the Resource <%= detail_resources_url(@resource.resource_type, @resource.id) %>

Feel free to explore and make the most of this new content.
