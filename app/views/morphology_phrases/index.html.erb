<%
  actions = []

  if @access
    actions << link_to('New Phrase', new_morphology_phrase_path, class: 'h-10 btn btn-success inline-flex items-center', data: { controller: 'ajax-modal', url: new_morphology_phrase_path(modal: true) })
    actions << link_to('View bookmarks', '#_', class: 'btn btn-dark btn-header-action', data: { controller: 'phrase-ayah-bookmark' })
  end

  if params[:proofread]
    actions << link_to('Phrase view', morphology_phrases_path, class: 'h-10 btn btn-info inline-flex items-center')
  else
    actions << link_to('Proofread view', morphology_phrases_path(proofread: true), class: 'h-10 btn btn-info inline-flex items-center')
  end
%>

<%= render 'tools/header', name: 'Mutashabihat ul Quran', actions: actions, key: 'mutashabihat' %>

<div class="page-wrapper container max-w-7xl mx-auto px-4">
  <% if params[:proofread] %>
    <%= render 'proofread_list' %>
  <% else %>
    <%= render 'phrase_list' %>
  <% end %>
</div>
