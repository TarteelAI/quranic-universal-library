<div id="resources">
  <%= render 'hero' %>
  <%= render 'layouts/grid' do %>
    <div class="grid-cols-subgrid grid mt-6">
      <div class="flex flex-col md:flex-row gap-3">
        <h2 class="font-semibold w-full md:w-1/3 text-3xl font-title">
          About QUL Resources
        </h2>
        <p class="w-full md:w-2/3 text-pretty">
          Below you will find a list of the categories of Quranic data available on QUL, for example translations,
          tafsirs, and surah information. Clicking on one of these resource categories will take you to a page where you
          can download the various resources belonging to that category in formats such as sqlite and json. For more
          granular options to find and download resources, visit the advanced admin panel. Resources included in QUL are
          intended for you to download and package with your own project; no API is available
        </p>
      </div>

      <div class="mt-8">
        <%= render 'content_notice' %>
      </div>

      <div class="mt-8 mb-10">
        <%= render 'resources_list' %>
      </div>
    </div>
  <% end %>
</div>
