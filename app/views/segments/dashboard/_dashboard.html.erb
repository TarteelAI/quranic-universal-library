<%
  data = @presenter.graph_data
%>

<div class="page-wrapper container max-w-7xl mx-auto px-4">
  <div class="page-section mt-4" data-turbo="false">
    <h1 class="text-2xl font-bold mb-4">Detection Statistics Dashboard</h1>

    <%= form_with url: segments_dashboard_path, method: :get, class: "flex gap-4 mb-6" do %>
      <div>
        <label class="block text-sm font-semibold">Reciter:</label>
        <select name="reciter_id" class="border rounded px-2 py-1">
          <option value="">All</option>
          <% @presenter.reciters.each do |reciter| %>
            <option value="<%= reciter.id %>" <%= 'selected' if @presenter.selected_reciter == reciter.id %>><%= reciter.name %></option>
          <% end %>
        </select>
      </div>

      <div>
        <label class="block text-sm font-semibold">Surah:</label>
        <select name="surah" class="border rounded px-2 py-1">
          <option value="">All</option>
          <% @presenter.segmented_surah.each do |s| %>
            <option value="<%= s %>" <%= 'selected' if @presenter.selected_surah.to_s == s.to_s %>>Surah <%= s %></option>
          <% end %>
        </select>
      </div>

      <div class="flex items-end">
        <button type="submit" class="px-4 py-2 rounded font-medium transition-colors bg-blue-400 hover:bg-blue-500 text-white">Filter</button>
      </div>
    <% end %>

    <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
      <div>
        <h2 class="font-bold mb-2">Detection Type Count</h2>
        <canvas id="detectionChart" height="250"></canvas>
      </div>

      <div>
        <h2 class="font-bold mb-2">Mistake Type Distribution</h2>
        <canvas id="mistakeChart" height="250"></canvas>
      </div>
    </div>

    <div class="mt-8">
      <h2 class="font-bold mb-2">Reciters Stats</h2>
      <div class="w-full">
        <canvas id="recitersChart" height="100"></canvas>
      </div>
    </div>
  </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
<script>
  const detectionData = {
    labels: <%= raw data[:detections].keys.map(&:humanize).to_json %>,
    datasets: [{
      label: 'Count',
      data: <%= raw data[:detections].values.to_json %>,
      backgroundColor: 'rgba(37, 99, 235, 0.6)'
    }]
  };

  const mistakeData = {
    labels: <%= raw data[:mistake_types].keys.map(&:humanize).to_json %>,
    datasets: [{
      label: 'Mistakes',
      data: <%= raw data[:mistake_types].values.to_json %>,
      backgroundColor: 'rgba(220, 38, 38, 0.6)'
    }]
  };

  new Chart(document.getElementById('detectionChart'), {
    type: 'bar',
    data: detectionData,
    options: {
      responsive: true,
      plugins: {legend: {display: false}},
      scales: {
        y: {beginAtZero: true}
      }
    }
  });

  new Chart(document.getElementById('mistakeChart'), {
    type: 'bar',
    data: mistakeData,
    options: {
      responsive: true,
      plugins: {legend: {display: false}},
      scales: {
        y: {beginAtZero: true}
      }
    }
  });

  const reciterStats = <%= raw @presenter.reciter_stats.to_json %>;

  const reciterChartData = {
    labels: reciterStats.map(r => r.name),
    datasets: [
      {
        label: 'Positions',
        data: reciterStats.map(r => r.positions),
        backgroundColor: 'rgba(34, 197, 94, 0.6)' // green
      },
      {
        label: 'Failures',
        data: reciterStats.map(r => r.failures),
        backgroundColor: 'rgba(239, 68, 68, 0.6)' // red
      }
    ]
  };

  new Chart(document.getElementById('recitersChart'), {
    type: 'bar',
    data: reciterChartData,
    options: {
      responsive: true,
      scales: {
        y: {
          beginAtZero: true
        }
      }
    }
  });
</script>

