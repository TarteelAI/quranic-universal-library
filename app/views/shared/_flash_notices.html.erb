<div id="flash-messages">
  <div class="tw-hidden"
       data-controller="flash-message"
       data-flash-error="<%= flash[:alert] || flash.now[:alert] %>"
       data-flash-notice="<%= flash[:notice] || flash.now[:notice] %>">
  </div>
</div>
