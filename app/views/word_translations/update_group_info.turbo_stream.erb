<%= turbo_stream.replace 'flash-messages' do %>
  <%= render 'shared/flash_notices' %>
<% end %>

<% @presenter.wbw_translations(@verse).each do |word_translation| %>
  <%= turbo_stream.update dom_id(word_translation.word, :group) do %>
    <%= render 'word_group_info', word_translation: word_translation, language: current_language, word: word_translation.word %>
  <% end %>
<% end %>
