# Audio processing / core
numpy>=1.24.0
scipy>=1.10.0
pandas>=2.0.0

# Librosa and related (mel/signal)
librosa>=0.10.0
resampy>=0.4.2
audioread>=3.0.0

# Sound file handling
soundfile>=0.12.1
pydub>=0.25.1

# Visualization / plotting
plotly>=5.6.0
matplotlib>=3.5.0

# Utilities (optional but helpful)
joblib>=1.2.0

# NOTE: numba can speed up librosa internals but sometimes causes build issues on hosted services.
# If you want, you can add numba; if build fails remove it and try again.
numba>=0.56.0